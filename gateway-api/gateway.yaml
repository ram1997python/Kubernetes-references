apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: jeevi-gateway
spec:
  gatewayClassName: example-gc
  listeners:
  - name: http
    port: 80
    protocol: HTTP
    allowedRoutes:
      namespaces:
        from: Same
