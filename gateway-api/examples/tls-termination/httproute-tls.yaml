apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: blog-route-tls
spec:
  parentRefs:
  - name: jeevi-gateway-tls
  hostnames:
  - blog.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: jeevi-blog
      port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: services-route-tls
spec:
  parentRefs:
  - name: jeevi-gateway-tls
  hostnames:
  - services.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: jeevi-services
      port: 80
